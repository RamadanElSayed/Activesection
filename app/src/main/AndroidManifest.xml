<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="soft.mycompany.activesection">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Myclass.yourApp"
        android:allowBackup="true"
        android:icon="@drawable/comp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DeptActivity" />
        <activity android:name=".EmpActivity" />
        <activity android:name=".SerachActivity" />
        <activity android:name=".ShowEmpActivity" />
        <activity android:name=".AllDtaAboutEmp" />
        <activity android:name=".Update_Activity"></activity>
    </application>

</manifest>